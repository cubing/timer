{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./node_modules/@cubing/dev-config/ts/es2022-types/tsconfig.json",
  "compilerOptions": {
    // Some types (imported from scripts) are broken.
    // PouchDB's implementation quality is… poor. So these types are likely to be broken for a long time.
    "skipLibCheck": true
  },
  "exclude": ["./dist"]
}
