"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertToQQTimerFormat = convertToQQTimerFormat;

async function convertToQQTimerFormat(session, eventId) {
  return (await session.allAttempts()).filter(attempt => attempt.event === eventId).map(attempt => attempt.totalResultMs).join(",") + "->";
}